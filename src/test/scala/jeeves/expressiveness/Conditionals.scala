package test.cap.jeeves

import cap.scalasmt._
import cap.jeeves._
import org.scalatest.FunSuite
import org.scalatest.Assertions.{expect}
import scala.collection.immutable.Map

/**
 * Making sure the right thing is happening under conditionals.
 * @author jeanyang
 */
class Conditionals extends FunSuite with JeevesLib {
  test("policies under conditionals") {
    // TODO: Figure out if we allow policies under conditionals.  If so, write
    // a test for it.
  }
}
